# Configure API keys in the app's Settings page.
# Settings, resume workspace, and encrypted API keys are stored in SQLite
# (rabbitinterview.db under the app data directory). Recordings stay as files.

# Optional
TAURI_DEV_HOST=
